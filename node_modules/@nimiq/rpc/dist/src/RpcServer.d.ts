import { State } from './State';
export { State, ResponseStatus } from './State';
export declare type CommandHandler = (state: State, ...args: any[]) => any;
export declare class RpcServer {
    private static _ok;
    private static _error;
    private readonly _allowedOrigins;
    private readonly _responseHandlers;
    private readonly _receiveListener;
    private _clientTimeout;
    constructor(allowedOrigins: string | string[]);
    onRequest(command: string, fn: CommandHandler): void;
    init(onClientTimeout?: () => void): void;
    close(): void;
    private _receiveRedirect;
    private _receive;
}

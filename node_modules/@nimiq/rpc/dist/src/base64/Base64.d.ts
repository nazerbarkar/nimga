export declare class Base64 {
    static byteLength(b64: string): number;
    static decode(b64: string): Uint8Array;
    static encode(uint8: Uint8Array): string;
    static encodeUrl(buffer: Uint8Array): string;
    static decodeUrl(base64: string): Uint8Array;
    private static readonly _lookup;
    private static _revLookup;
    private static _initRevLookup;
    private static _getLengths;
    private static _byteLength;
    private static _tripletToBase64;
    private static _encodeChunk;
}

import { ObjectType } from './ObjectType';
export declare class RequestIdStorage {
    static readonly KEY: string;
    private static _decodeIds;
    private readonly _store;
    private _validIds;
    /**
     * @param storeState Whether to store state in sessionStorage
     */
    constructor(storeState?: boolean);
    has(id: number | string): boolean;
    getCommand(id: number | string): string | null;
    getState(id: number | string): ObjectType | null;
    add(id: number, command: string, state?: ObjectType | null): void;
    remove(id: number | string): void;
    clear(): void;
    private _encodeIds;
    private _restoreIds;
    private _storeIds;
}

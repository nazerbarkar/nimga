import { PostMessage, RedirectRequest, ResponseStatus } from './Messages';
export { ResponseStatus } from './Messages';
export declare class State {
    readonly id: number;
    readonly origin: string;
    readonly data: {
        command: string;
        args: any[];
        id: number;
    };
    readonly returnURL: string | null;
    readonly source: string | MessagePort | ServiceWorker | Window | null;
    static fromJSON(json: string): State;
    private readonly _origin;
    private readonly _id;
    private readonly _responseMethod;
    private readonly _returnURL;
    private readonly _data;
    private readonly _source;
    constructor(message: MessageEvent | RedirectRequest | PostMessage);
    toJSON(): string;
    reply(status: ResponseStatus, result: any): void;
    toRequestObject(): RedirectRequest;
}
